#include "main.h"

/**
 * _isalpha - checks for a letter
 * @c: could be a letter or not
 * Return: 1 if letter and 0 otherwise
 */
int _isalpha(int c)
{
	if (i >= 65 && i <= 90)
		return (1);
	else if (i >= 97 && i <= 122)
		return (1);
	else
		return (0);
}
